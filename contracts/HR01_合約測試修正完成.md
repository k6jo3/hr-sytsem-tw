# HR01 IAM 服務 - 合約測試修正完成報告

**日期：** 2026-02-11
**狀態：** ✅ 測試文件已修正完成

---

## 📋 修正摘要

根據合約文件 `iam_contracts.md` 對測試文件 `IamApiContractTest.java` 進行了完整修正。

### 修正原則

**合約文件 = 唯一的測試規格來源**
- 合約文件定義的場景 → 測試必須實作
- 合約文件沒有的場景 → 測試必須刪除

---

## 🗑️ 刪除的測試（2 個）

以下測試在合約文件中沒有定義，已刪除：

| 測試方法 | 原因 |
|:---|:---|
| `searchByKeyword_AsAdmin()` | ❌ 合約文件沒有 IAM_QRY_002（按關鍵字搜尋使用者） |
| `searchByRole_AsAdmin()` | ❌ 合約文件沒有 IAM_QRY_003（按角色搜尋使用者） |

---

## ✅ 保留的測試（7 個）

以下 Query 操作測試與合約文件對應，已保留：

| 測試方法 | 合約場景 | 說明 |
|:---|:---:|:---|
| `searchActiveUsers_AsAdmin()` | IAM_QRY_001 | 查詢啟用的使用者 |
| `searchLockedUsers_AsAdmin()` | IAM_QRY_004 | 查詢鎖定的使用者 |
| `searchByTenant_AsSuperAdmin()` | IAM_QRY_005 | 按租戶查詢使用者 |
| `searchAllRoles_AsAdmin()` | IAM_QRY_101 | 查詢所有角色 |
| `searchRolesByName_AsAdmin()` | IAM_QRY_102 | 按名稱查詢角色 |
| `searchSystemRoles_AsAdmin()` | IAM_QRY_103 | 查詢系統角色 |
| `searchCustomRoles_AsAdmin()` | IAM_QRY_104 | 查詢自訂角色 |

---

## 🆕 新增的測試（7 個）

根據合約文件中的 Command 操作，新增以下測試：

### 使用者管理 Command（4 個）

| 測試方法 | 合約場景 | 說明 | 驗證內容 |
|:---|:---:|:---|:---|
| `createUser_AsAdmin_IAM_CMD_001()` | IAM_CMD_001 | 建立使用者 | ✅ 資料寫入 users/user_roles<br>✅ UserCreatedEvent |
| `updateUser_AsAdmin_IAM_CMD_002()` | IAM_CMD_002 | 更新使用者 | ✅ Email 更新<br>✅ UserUpdatedEvent |
| `deactivateUser_AsAdmin_IAM_CMD_003()` | IAM_CMD_003 | 停用使用者 | ✅ status = INACTIVE<br>✅ UserDeactivatedEvent |
| `activateUser_AsAdmin_IAM_CMD_004()` | IAM_CMD_004 | 啟用使用者 | ✅ status = ACTIVE<br>✅ UserActivatedEvent |

### 角色管理 Command（3 個）

| 測試方法 | 合約場景 | 說明 | 驗證內容 |
|:---|:---:|:---|:---|
| `createRole_AsAdmin_IAM_CMD_101()` | IAM_CMD_101 | 建立角色 | ✅ 資料寫入 roles/role_permissions<br>✅ RoleCreatedEvent |
| `updateRole_AsAdmin_IAM_CMD_102()` | IAM_CMD_102 | 更新角色 | ✅ 名稱/描述更新<br>✅ RoleUpdatedEvent |
| `deleteRole_AsAdmin_IAM_CMD_103()` | IAM_CMD_103 | 刪除角色（軟刪除） | ✅ is_deleted = true<br>✅ RoleDeletedEvent |

---

## 📊 修正前後對比

| 項目 | 修正前 | 修正後 | 變化 |
|:---|:---:|:---:|:---:|
| **Query 測試** | 9 個 | 7 個 | -2（刪除不符合的） |
| **Command 測試** | 0 個 | 7 個 | +7（新增） |
| **總計** | 9 個 | **14 個** | +5 |
| **與合約一致性** | ⚠️ 部分不一致 | ✅ 完全一致 | 100% |

---

## 🔧 技術細節

### 新增的 import

```java
import org.springframework.http.MediaType;
import com.company.hrms.common.test.contract.ContractSpec;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.HashMap;
import java.util.Map;
```

### 使用的驗證方法

#### Query 測試（使用現有方法）
```java
assertContract(queryCaptor.getValue(), contractSpec, "IAM_QRY_001");
```
- 驗證 QueryGroup 的過濾條件
- 向下相容原有測試

#### Command 測試（使用新方法）
```java
// 1. 載入合約
ContractSpec contract = loadContract("iam_contracts", "IAM_CMD_001");

// 2. 擷取快照
Map<String, List<Map<String, Object>>> beforeSnapshot = captureDataSnapshot("users");
Map<String, List<Map<String, Object>>> afterSnapshot = captureDataSnapshot("users");

// 3. 驗證完整合約
verifyCommandContract(beforeSnapshot, afterSnapshot, capturedEvents, contract);
```
- 驗證資料異動（INSERT/UPDATE/DELETE/SOFT_DELETE）
- 驗證領域事件發布
- 完整的輸入 + 輸出 + 副作用驗證

---

## 📝 測試結構

### 三個測試類別

```
IamApiContractTest
├── UserQueryApiContractTests (5 個測試)
│   ├── searchActiveUsers_AsAdmin()
│   ├── searchLockedUsers_AsAdmin()
│   ├── searchByTenant_AsSuperAdmin()
│   └── ...
├── RoleQueryApiContractTests (2 個測試)
│   ├── searchAllRoles_AsAdmin()
│   ├── searchRolesByName_AsAdmin()
│   └── ...
├── UserCommandApiContractTests (4 個測試) ⭐ 新增
│   ├── createUser_AsAdmin_IAM_CMD_001()
│   ├── updateUser_AsAdmin_IAM_CMD_002()
│   ├── deactivateUser_AsAdmin_IAM_CMD_003()
│   └── activateUser_AsAdmin_IAM_CMD_004()
└── RoleCommandApiContractTests (3 個測試) ⭐ 新增
    ├── createRole_AsAdmin_IAM_CMD_101()
    ├── updateRole_AsAdmin_IAM_CMD_102()
    └── deleteRole_AsAdmin_IAM_CMD_103()
```

---

## ⚠️ 待辦事項

### 1. 實作 API 端點

Command 測試需要對應的 API 端點實作：
- ⏳ `POST /api/v1/users` - CreateUserServiceImpl
- ⏳ `PUT /api/v1/users/{id}` - UpdateUserServiceImpl
- ⏳ `PUT /api/v1/users/{id}/deactivate` - DeactivateUserServiceImpl
- ⏳ `PUT /api/v1/users/{id}/activate` - ActivateUserServiceImpl
- ⏳ `POST /api/v1/roles` - CreateRoleServiceImpl
- ⏳ `PUT /api/v1/roles/{id}` - UpdateRoleServiceImpl
- ⏳ `DELETE /api/v1/roles/{id}` - DeleteRoleServiceImpl

### 2. 實作事件監聽器

需要建立事件監聽器來捕獲領域事件：
```java
@Component
public class TestEventListener {
    private List<Map<String, Object>> capturedEvents = new ArrayList<>();

    @EventListener
    public void onUserCreated(UserCreatedEvent event) {
        // 記錄事件
    }

    public List<Map<String, Object>> getCapturedEvents() {
        return capturedEvents;
    }

    public void clear() {
        capturedEvents.clear();
    }
}
```

### 3. 資料庫初始化

Command 測試需要測試資料庫和初始資料：
- ⏳ 使用 Testcontainers 建立 PostgreSQL 測試容器
- ⏳ 執行資料庫 schema 初始化
- ⏳ 準備測試資料（使用者、角色、權限）

---

## 🚀 執行測試

### 執行所有測試（14 個）

```bash
cd D:\git\hr-system2\hr-sytsem-2\backend\hrms-iam
mvn test -Dtest=IamApiContractTest
```

### 只執行 Query 測試（7 個）

```bash
mvn test -Dtest=IamApiContractTest$UserQueryApiContractTests
mvn test -Dtest=IamApiContractTest$RoleQueryApiContractTests
```

### 只執行 Command 測試（7 個）

```bash
mvn test -Dtest=IamApiContractTest$UserCommandApiContractTests
mvn test -Dtest=IamApiContractTest$RoleCommandApiContractTests
```

---

## ✅ 完成檢核表

- [x] 刪除不在合約文件中的測試（2 個）
- [x] 保留合約文件中的 Query 測試（7 個）
- [x] 新增合約文件中的 Command 測試（7 個）
- [x] 測試結構符合合約文件
- [x] 使用正確的驗證方法
- [ ] API 端點實作
- [ ] 事件監聽器實作
- [ ] 測試資料庫設定
- [ ] 執行並通過所有測試

---

## 📚 相關文件

- 合約文件：`contracts/iam_contracts.md`
- 測試文件：`backend/hrms-iam/src/test/java/com/company/hrms/iam/api/contract/IamApiContractTest.java`
- 測試基類：`backend/hrms-common/src/test/java/com/company/hrms/common/test/contract/BaseContractTest.java`
- 規範手冊：`contracts/合約測試規範手冊.md`

---

**建立者：** Claude Sonnet 4.5
**狀態：** ✅ 測試文件修正完成，待 API 實作和測試執行
